# PAIN — 阅读体验痛点收集

Charlie 在门下读折时的真实卡点，按发生记录，一行一个瞬间。
这是阅读器迭代的需求池：修某条前先来这里看它是不是一类问题。

| 日期 | PR | 具体瞬间 |
|---|---|---|
| 2026-07-30 | #46 | 代码 review 折：§4 是 ~200 行裸 unified diff，阅读器没有 diff 高亮，`+`/`-`/`@@` 全成视觉噪音——原话「一点渲染没有啊，很难看啊」。当次以 v2 绕过（改成普通代码块+`← 新增`批注）；根治要 diff 渲染（±行着色，或折叠 hunk/并排视图）。**已修**：`render.js` 给 ```` ```diff ```` 围栏加专用渲染，`+`/`-`/`@@` 行各自上色（整行浅底 + 左缘色条），暗色下另提亮一档；`html:false` 不动——每行自己 `escapeHtml` 后包 `<span>`，是生成常量不是放行用户 HTML（`render-safety.test.js` 加了「diff 里的 `<script>` 仍被转义」守卫）。hunk 折叠/并排视图未做（±着色已达标） |
| 2026-08-05 | #86 | 长折里前后跳转不了：正文写「抢中就插入下面的〈Highline 日模块〉」，读到这句时那个模块还在下面几屏外，原话「？？？ 选项呢 原来在下面」。他直接提了两个需求——**页内锚点跳转**和**回到顶部 / 回到底部的悬浮按钮**；折越长越致命（#86 正文 189 行）。已修：标题按 GitHub 口径打 id、`[…](#slug)` 拦成篇内滚动＋闪现（指不着的锚出声，不静默）；`.scroll-ends` 两个常驻悬浮钮，滚谁按「量得出溢出」判所以手机壳也对。**未做**：`[[别篇#某节]]` 跨篇锚点点击只切篇不落锚（`link.js` 把片段切掉了） |
| 2026-08-06 | #55 | 图点不开：mermaid 以 `-s 3` 渲成两三千像素宽的 PNG，进折被 CSS 缩到栏宽，**没有任何放大入口**——原话「图不能点开放大」。叠加 SPEC 已认下的那条代价「图片不能划句涂归」，图在手机上既点不开也批不了，**退化成装饰**；而 #55 这折押了六张架构图。修法：点图开灯箱（原尺寸＋捏合缩放＋Esc 关）。注意 `render.js` 是 `html:false`，不能靠内嵌 `<a>`，得挂在 `hydrateRelativeImages`（私有仓图本来就在那儿换 blob URL）。与 #46 同源：**这个阅读器对"非文字块"一直是二等公民**——diff 不渲染、图不能放大不能批 |
| 2026-08-06 | 通用 | **做了 ≠ 找得到**：到顶/到底的钮上线一天，他原话「在最新的一百折里面，我没找到怎么『到底』和『到顶』」。功能是好的、线上也验过，但摆在视口右下角、34px 半透明、只有箭头字形——量下来离正文列 623px。教训写死在这儿：**这个阅读器的控件，离正文越远＝越不存在**；「不打扰阅读」和「找得到」冲突时，先保后者。已修：贴正文左缘、垂直居中、实心底；位置量 `#doc` 而不是算 `--rail-w`（版心居中，屏越宽差越大） |
| 2026-08-06 | 通用 | 判（总判）写不成：他「很多时候是看到最底下才想给个总判，但总判的窗口永远固定在最开头」。更糟的是旧实现方向是反的——按下「判」用 `scrollIntoView` 把**人**拽到卡片那儿去（2026-08-02 为治窄屏「判不了」加的），刚读到的那段当场丢失。已修：把**卡**送到人眼前，fixed 钉在视口垂直中央、横向对齐眉批栏，边读边写；窄屏那条老病一并根治。同源于 #86：**长折里「我在哪」是第一位的，任何替用户滚屏的好意都是在偷走它** |
| 2026-08-02 | #66 | 读折时对不上号：agent 在会话里一律用「#66」指代，顶栏与正文却只有标题，编号只活在 URL 的 `?pr=` 里——原话「你天天说编号，但是这些文档根本没有编号只有url里有我看起来很不方便」。清单页其实有编号（`.meta` 小字），**一打开折就消失**，而读正文正是停留最久的地方。已修：面包屑补 `#<号>`（`topbar.js` + `.crumb-no`）。同轮扫出同类漏网：`strings.js` 的 `crumbRoot`/`tabOpen` 在 7/31 改名时漏改，屏幕上仍是旧词「待批」而 agent 嘴里说「待审」——同一个病根：**agent 的词汇与屏幕上的字不一致**，一并修掉 |
| 2026-08-06 | #86 | 长折里前后跳转不了：正文写「抢中就插入下面的〈Highline 日模块〉」，他读到这句时那个模块还在下面几屏外，原话「？？？ 选项呢 原来在下面」。他直接提了两个需求——**页内锚点跳转**（点标题引用就跳过去）和**回到顶部 / 回到底部的悬浮按钮**。文档层面能靠 markdown 锚点缓解一半，但「回到顶部/底部」只有阅读器能做；折越长这个越致命（#86 正文 189 行） |
| 2026-08-09 | #31 | 一本 22 章的书打开只见一篇，翻不了章——原话「里面怎么只有这篇 MD？我的整本书呢？我想在那边读整本书啊」。**根因是 CSS 层叠**：`.chapter-rail` 的 `display:flex` 写在文件第 47 行的 `@media(min-width:1940px)` 里，而第 867 行的基础规则又写了 `display:none`，同特异度后来居上——章栏**从上线那天起一次都没显示过**；同一个 @media 还把兜底的 `.chapters` 关掉，于是 ≥1940px 两个入口一起消失（他 2005px，正在死区）。已修 `6d8e140`（display 开关挪到组件规则旁 + `test/css-cascade.test.js` 两道守卫）。**但读者侧的账没销**：① 窄屏下那个章下拉是**圆角胶囊 + 灰色小字 + 默认收起**，一本 22 章的书打开仍然只看得见「这一篇」，得先知道那里可以点——同一晚他也没找到折卡上的体例检查绿钩（`.chk` 角标同样是「小到要人主动去找」）。**两处同病：信息在，但视觉重量配不上它的重要性。** ② 「翻章」和「连续读完整本」不是一回事，他要的可能是后者，目前无解。**残账①已修**：章下拉 `.chapters-summary` 加朱砂左缘 + 实底 + 深色书名与「第 i / n 章」标签（同 `folder-body.has-decisions` 用朱砂提示重量的手法），「这里还有整本」在余光里就现出来；`.chk` 角标放大到近正文字号、加粗、套浅色药丸底，绿钩/红叉一眼看得见，不必先知道它在那儿。残账②（连续读完整本）仍无解 |
| 2026-08-09 | #102 | 页内锚点整套是废的——原话「这里的渲染有点小问题，下次改吧」，指向正文里裸露的 `<a id="daily"></a>`。**难看只是表症，真问题是 #86 那条需求根本没修上**：写侧为了满足「页内跳转」给全文加了 5 个 `<a id>` 锚点，而阅读器三层全缺——① `render.js:4` 的 `markdownit({html:false})` 不是丢弃 raw HTML 而是**转义成可见文本**（于是 5 行垃圾）；② 渲染管线**不给标题生成 slug id**（没挂 anchor 插件）；③ 全文无 `hashchange`/`scrollIntoView`，**hash 链接点了不动**。结果 #102 里 `[已否决的方案](#rejected)` 等 4 条跳转全死。`html:false` 是承重的 XSS 防线不能开，正解＝渲染前剥掉「只含锚点的裸 HTML」（`folder-body.js` 剥「回奏对」注释已有同款落点）＋ 补标题 slug ＋ hash 滚动。**与 #86 并成同一条账**：写侧自以为交付了的功能，在读侧从未存在过，而两边都没人验过一次。**已修**：三层里②标题 slug（`heading_open` 打 id）与③ hash 滚动此前已随 #86 落地（`2acba68`，冒烟 `anchor-*` 一直在验）；这次补上①——`render.js` 的 `stripBareAnchors` 在渲染前剥掉「只含锚点、无可见文字」的 `<a id>`/`<a name>`（换等长空白保行号，同 `stripFrontmatter`），带可见文字的 `<a href>` 一律不碰。`html:false` 不动。`render-safety.test.js` + 冒烟 `bare-anchor-*` 双层守 |
