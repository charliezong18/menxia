# 架构切换观察表

零构建（vanilla JS）是拍板的架构（SPEC §8）。它什么时候到顶，由 SPEC §8.1 的指标判定：**触发任意两条 = 暂停功能开发，先做 A→B（Preact + Vite）迁移**；只触发一条 = 记录观察，不动架构。

每次开发结束顺手量一遍，超标的写进下面。

| # | 指标 | 触发线 |
|---|---|---|
| 1 | 单文件行数 | > 800 |
| 2 | 应用总行数（不含 vendor） | > 2500 |
| 3 | 单页手写 DOM 更新点（innerHTML / appendChild / 属性同步 **三类合计**） | > 25 |
| 4 | 跨页同步的独立状态 | > 6 |
| 5 | 同类「界面没跟上数据」bug | 修 ≥ 3 次 |
| 6 | 加一个小功能要改的文件数 | ≥ 4 **且总源文件数 > 8** |
| ~~7~~ | ~~首屏加载~~ | **已删**：零依赖静态页摸不到，换框架也不治 |
| 8 | 异步竞态守卫（手写「过期响应丢弃/取消」的位置） | > 3 处 |
| 9 | 乐观更新回滚路径 | > 2 个 |

## 量测记录

| 日期 | 里程碑 | 最大单文件 | 总行数 | DOM 更新点 | 状态数 | 竞态守卫 | 超标项 | 结论 |
|---|---|---|---|---|---|---|---|---|
| 2026-07-26 | M1 | style.css 158 / app.js 175 | 393 → 534 | ~15（首次记录只数了 innerHTML=8，口径已更正） | 4 | 1（openPR 世代计数） | 无 | 继续 A |
| 2026-07-26 | M2 | annotate.js 392 | 1007 | **~40（grep 三类合计，超线 #3=25）** | 6（贴线 #4） | 2（openPR 世代 + submit busy） | **#3 已触发（1 条）** | 按规矩：单条只记录不迁移。判定说明：40 个更新点集中在显式 render() 重建路径里（buildCard 等构造器内的 append 逐个计），不是散落的增量补丁——复杂度形态比数字温和。M3（批注串+乐观更新）大概率再触发 #5 或 #9，届时两条齐 → 迁移 |
| 2026-07-26 | M2 评审修复后 | annotate.js ~440 | ~1080 | ~42（#3 持续超线） | 6 | **4：openPR 世代 / openDoc 每 tab 换代 / submitAll 完成快照 / busy（#8 超线 >3）** | **🔔 #3 + #8 双触发 → 闸门成立** | **按 §8.1 规则：暂停新功能，M3 开工前先做 A→B（Preact+Vite）迁移。** 评审背书：本轮两个真 bug（⌘Enter 冒泡直发、完成回调不校验世界）全是事件传播与异步时序类——正是框架接管的那层心智负担。可搬资产：锚定数学、hunk 解析、GitHub API 封装、全部 CSS |
| 2026-07-26 | **迁移完成（当晚）** | ui.js 487 | 880（-200） | ~7（仅文档岛屿 innerHTML + layoutCards 定位——其余全归 vdom） | 同前（改由 hooks 承载） | 手写守卫 3（openPR стale 检查 ×2 + submit 快照）；文档加载竞态改由 effect cleanup 结构性解决 | 闸门已执行 | 形态：htm/preact standalone vendored（13KB 单文件），**保零构建**：push 即部署、无 npm 依赖链；Vite 留到真需要 JSX/TS 再上（与 SPEC §8 B 的字面差异已记入 §8 修订）。全部评审修复保留；`?demo=1&auto=1` 真实事件路径冒烟通过（fence 行号 16 精确）。指标继续按表监测，阈值不变 |
| 2026-07-26 | **M3（批注串+回话展示 / rev 版本切换）** | ui.js **641**（< 800，#1 未触发） | 1164（含 render.js；不含 vendor，< 2500，#2 未触发） | **16**（grep 三类合计仅 ui.js；**第四轮评审复核：M3 净增 0**——开工前同口径已是 16，13→16 涨于评审修复期，迁移行的 ~7 系口径漂移；< 25，**#3 未触发**） | App 内 useState 17（新增 4：commits/comments/viewed/otherOpen）——但需**跨页同步**的仍是 cur/docPath/drafts 一档，新增 4 个全是同页派生态，#4 实义未触发 | 手写守卫仍 3 档：文档岛屿 dead 旗（现依赖含 viewed，切 rev 复用同一 cleanup）＋ openPR/loadComments/submitAll 的 `cur.pr.number` 世界快照＋submit busy；批注串刷新走 loadComments 内联世界守卫，未新增独立守卫类型 | **M3 新增 0；第四轮评审复核：#8 按原「处」口径 5-6 处仍超线、#4 按总 state 口径 17>6 超线**——闸门已执行、B 之上无更高档，无进一步动作；两指标口径 v1 前重定义 | **诚实结论：闸门备忘预期 M3 会触发 #3/#5/#9——实际一条没触发，因为迁移已把「手写 DOM 同步」这层成本结构性消掉了（正是迁 Preact 的目的）。M3 的批注串+rev 全走 vdom 声明式渲染，DOM 更新点没涨（净增 0）。#9 乐观回滚：M3 的「提交后重拉批注串」是**读刷新**不是乐观写，无回滚路径，不计入 #9。5 条 smoke 全绿（含 rev 切旧版正文变短+浮批禁用）。阈值不变，继续按表监测 |
| 2026-07-27 | 搜索 + 归档 + 总批展示 + 设置修复（用户反馈日） | ui.js 802（**#1 贴线 2 行，802>800 已技术性触发**） | 1585 | 未重数（增量全走 vdom 声明式 + search.js 纯逻辑抽层） | +4（q/hits/searching/tab 等，均同页派生态） | 同前 | **#1 触发（1 条）** | 单条只记录不迁移（规矩）。ui.js 破 800 的对策不是换架构而是继续抽层：搜索编排已在 search.js，下一个功能动工前先把侧栏/阅读区拆成子组件文件。B 之上无更高档，此指标此后改为「监控拆层纪律」用 |

## 备忘

- 指标 8/9 是双路 review 后补的：七条旧指标全在测**静态复杂度**（行数/更新点/状态数），而真正逼人上框架的是**并发写**。openPR 的竞态今天已经用掉第 1 处。
- 不管迁不迁都该先做的对冲：单一 state 对象 + 显式 `render()` + 世代计数守卫（约 50 行）。迁移真正贵的部分是把状态从事件闭包里抠出来，这笔钱两头都得花。
- 预期闸门最可能在 **M3**（批注串 + 提交后刷新 + 乐观标记）触发指标 3/5/9——那不是失败，正是 §8.1 设计的用法。
