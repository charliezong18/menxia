# BACKLOG

已想清楚但还没排期的东西。排期规矩：先做完 M2/M3 闭环，再动这里——批注循环没跑顺之前加智能，是给没地基的房子装电梯。

---

## F1 · 折首摘要（TL;DR）

**要什么**：每折开头自动生成一段摘要——不管这折从哪儿 feed 进来（spec / issue / PR / 日报），进朱批之前先有个"这折说什么、要你拍什么"。

**为什么**：长文档最大的摩擦不是读不动，是**不知道值不值得读**。摘要把"要不要现在读"这个决定的成本降到 10 秒。

**怎么做**：agent 侧生成（review-loop skill 在开 PR 时就写好），存进 PR body 或文档 frontmatter；朱批只负责渲染成折首的摘要卡。**app 内不调 LLM**（见下方架构边界）。

**待定**：摘要要不要分两段——「这折说什么」+「要你拍什么」（后者其实就是现在 PR body 的「待你拍板」，可以直接升级成结构化字段）。

---

## F2 · 臣拟朱批（agent 按你的 background 先粗筛一遍）

**要什么**：文档呈上来之前，agent 先以「Charlie 的视角」通读一遍——他是程序员、在做 X 项目、关心 Y——把可能的问题预先批在原文对应的句子上。你打开时，右缘已经有一列灰色的「臣拟」批注等着。

**为什么**：这是这个产品**唯一一件 Google Docs、GitHub、Notion 都做不到的事**。它们能给你更好的批注 UI，但没有一个能在你读之前替你读一遍。你的时间瓶颈从来不在"写批注"，在"发现该批哪里"。

**怎么做**：agent 在呈折时顺带跑一遍 background-aware review，把结果作为**普通 inline comment** 发到 PR 上，正文带 `臣拟` 标记（比如首行 `> 臣拟`）。朱批渲染时识别这个标记，用灰底、比真朱批低一档的视觉呈现。**app 依旧不调 LLM**。

**背景从哪来**：一份 `background.md`（职业、在做的项目、技术偏好、review 时最在意什么），放 review repo 里，agent 每次读它。这份东西 memory 里已经有大半，可以直接导出。

**风险（要防的）**：预填批注会**锚定你的判断**——人看见现成意见容易顺着点头，反而少想。缓解：臣拟批注默认折叠成一行标题，你读完自己那遍再展开；或者提供"先蒙上"开关。这条不解决就别做 F2。

---

## F3 · 一键认同 / 加一

**要什么**：臣拟批注旁边给按钮——「准」（认同，转成正式朱批呈回）、「加一」（在它基础上追一句你的话）、「驳」（不认同，丢弃）。不用自己敲字。

**为什么**：F2 的价值只有在**采纳成本≈0** 时才兑现。要你重新打一遍字，等于没帮上忙。

**怎么做**：纯前端 + GitHub API——「准」= 把臣拟 comment 的内容原样重发一条正式朱批（或直接给原 comment 加 👍 reaction + 标记为已采纳）；「加一」= 打开批注框并预填引用；「驳」= 本地隐藏 + 记一次负反馈（可用于改进 F2 的 prompt）。

**待定**：「准」是转成你名下的新批注，还是给臣拟批注盖个"御准"戳？前者对 agent 更清晰（它只认正式朱批），后者更省事。倾向前者。

---

## 架构边界（F1–F3 共同前提，先定死）

SPEC §3 的非目标写着「AI 集成——agent 侧由 review-loop skill 全权负责，朱批不知道 AI 存在」。F1–F3 看似违反它，实则不必：

**所有智能都在 agent 侧离线算好，以普通 GitHub 数据（PR body / inline comment）的形式送进来；朱批只负责识别标记并渲染成合适的样子。**

这样守住三件事：①朱批仍是零后端纯静态站，不需要任何 API key 在浏览器里；②agent 换模型、换 prompt 不影响前端；③朱批挂了，臣拟批注照样在 GitHub 上看得见。

代价：不能做"读到一半问一句"这种实时交互——那要 app 内调 LLM，是另一个量级的产品（要后端或要用户自带 API key）。真需要时，走 SPEC §8.1 里 C 方案的触发条件评估。

---

## F4 · 版本（rev）浏览与版本间 diff（2026-07-26 口头提及，先对齐记录）

**记录澄清**：拍板②定的是 v0 **不做**「v1→v2 对比视图」；北极星的 diff 奏折看的是代码 PR 的新旧两侧，不是文档版本对比。所以「看 rev / diff between revs」目前**不在**任何已排期的里程碑里——但地基已经在：每轮改稿都是 PR 分支上的一个 commit，`getFileText(path, ref)` 本来就支持任意 commit SHA，缺的只是 UI 入口。

三档实现，从便宜到贵：

1. **rev 切换**（~小时级）：用 PR commits API 列出历史版本，阅读页加版本下拉，切到任意旧版渲染。只读不比对。——**✅ 2026-07-26 拍板划进 M3**，不再属于 backlog。
2. **版本间源码 diff**（~半天）：任选两个 rev，复用北极星的 split-view 组件展示 markdown **源码** diff。组件白嫖，代价是看的是源码态（阅读体验降一档，胜在便宜）。
3. **渲染态 track-changes**（贵）：改动段落在渲染态上高亮 = 原拍板②的 B 方案，要做段落级 diff 算法 + UI。

**第 2/3 档触发条件**：rev 切换上线后，真实使用中「翻旧版也找不到改动点」发生 ≥3 次再上第 2 档；第 3 档除非前两档都被证明不解渴。

---

## F5 · 派人复核与签字（assign reviewer + sign-off）

**要什么**：把一折奏折指派给别人（同事 / 惠雪 / larry / 未来的团队）读并**签字放行**，签完才允许钦此。从「单人 review 台」长成「可问责的评审流程」。

**为什么现在记而不做**：v0 的全部设计前提是单人（朱批/总批/钦此都是"你一个人的动作"），派人复核会同时动到身份模型、权限模型和 UI 语义——不是加个按钮的事。但这是产品从"我的工具"走向"能给别人用"的第一道门，值得留位置。

**有意思的反转**：整个「钦此」按钮之所以存在，是因为 **GitHub 不允许作者批准自己的 PR**（试点第一天就撞见，PAIN.md 第一条）。而**别人是可以 Approve 的**——所以 F5 一旦落地，GitHub 原生的 review approval 机制立刻可用：签字 = 一条 `event: 'APPROVE'` 的 review，状态天然可查（`GET /pulls/{n}/reviews` 的 state=APPROVED）。**不需要发明自己的签字存储**，仍守着「智能/状态全在 GitHub、朱批只是镜片」的架构边界。

**怎么做（草案）**：
- 指派：`POST /pulls/{n}/requested_reviewers`（需要对方有仓库读权限——私有仓要么加 collaborator，要么这类协作折走另一个仓）。UI：折子头部一个「请…复核」。
- 签字：被指派人在自己的朱批里读完 → 一个「准奏」按钮 = `event: 'APPROVE'`；驳回 = `REQUEST_CHANGES`。这两个 event 值 submitReview 已经支持，只差 UI。
- 状态：清单页每折显示签字态（待你批 / 待某某签 / 已准奏 N/M）；**钦此按钮在「要求签字」的折上未签不给按**（软闸，本地判断，不是 branch protection）。
- 硬闸可选：真要不可绕过就上 GitHub branch protection 的 required approvals——那是仓库设置不是 app 功能，一行不用写。

**待拍板（真做时才问）**：①身份——别人用自己的 GitHub token 打开同一个朱批站点即可（app 无用户系统，天然多人），还是要认"当前用户是谁"来决定按钮？（前者几乎零成本：`GET /user` 拿 login 就够）②私有 review 仓怎么给外人读权限，还是协作折另开一个仓？③签字是不是必须——默认可选、只有标记了「需签字」的折才卡钦此。

**排在哪**：v1（移动端 + PWA）之后、北极星（diff 奏折）附近；单人流程被真实使用磨顺之前不动。

---

## F6 · 折间链接（2026-07-27 ✅ 已做）

**做了什么**：文档/批注里的**本仓 GitHub permalink** 被朱批拦下来，变成 app 内跳转（开折 + 滚到那一段 + 闪一下）；外链照常新窗口打开。顶栏「引用此处」把当前选中处拷成 `[「引文」](permalink)`，粘进另一条朱批即成链接。

**为什么用 GitHub 原生 permalink 而不是自创 `[[双链]]`**：私有语法会让 GitHub 上显示成乱码、agent 还得学它，"数据在 GitHub、朱批只是镜片"这条根就断了。用 permalink：GitHub 上是正常链接、agent 读得懂、朱批只是把它**拦下来**——链接图谱不是新数据，只是 markdown 里的普通 URL。

**明确没做的：内嵌预览**（悬停/展开直接看到目标内容）。风险是递归（A 嵌 B、B 嵌 A）与快照失效（目标改版后嵌的是旧的还是活的）。等链接真被用起来再说。

**已知取舍**：链接会随目标改版漂移——所以「引用此处」拷的是**带引文的链接**，漂了至少还知道当初指的是哪句（同锚定的引文兜底哲学）。
