fence 内第一行代码
fence 内第二行代码 → 精确到该行（fence 首行 +1 +换行数）
fence 行号落在 <code> 上且指向 ``` 行
h1 行号
h2 行号
table 行号
划中内联格式内的字仍锚到源行（文本搜索会失败，DOM 主锚不会）
列表项行号
取该行之前最近的标题
块级元素带 data-line-end
引文不存在时返回 null（不乱画高亮）
引文取渲染文本（不含 markdown 记号）
撞词：按块内偏移定位到第二处而非永远第一处
无选区 → 不产生锚
段落内划选 → 段落行号
段落行号
第一节之内取 h1
第二个表格行独立成行
缩进代码块第二行 → 精确到该行（+0 偏移）
缩进代码块行号落在 <pre> 上且是首行
表格 tr 有独立行号（缺它则批表格只能锚到整表首行）
表格第二行划选 → 该行行号
