**English** · [中文](MIGRATION-WATCH.zh-CN.md)

# Architecture switch watchlist

Zero-build (vanilla JS) is the decided architecture (SPEC §8). When it runs out of road is decided by the indicators in SPEC §8.1: **any two tripped = feature work pauses and the A→B (Preact + Vite) migration happens first**; only one tripped = record the observation, leave the architecture alone.

Measure at the end of every development session and write anything over threshold into the log below.

| # | Indicator | Threshold |
|---|---|---|
| 1 | Lines in a single file | > 800 |
| 2 | Total app lines (excluding vendor) | > 2500 |
| 3 | Hand-written DOM update sites on one page (innerHTML / appendChild / attribute sync, **all three categories combined**) | > 25 |
| 4 | Independent state that has to sync across pages | > 6 |
| 5 | Repeat "the view didn't keep up with the data" bugs | fixed ≥ 3 times |
| 6 | Files touched to add one small feature | ≥ 4 **and total source files > 8** |
| ~~7~~ | ~~First paint~~ | **Deleted**: a zero-dependency static page can't reach this line, and a framework wouldn't cure it anyway |
| 8 | Async race guards (hand-written "discard stale response / cancel" sites) | > 3 sites |
| 9 | Optimistic-update rollback paths | > 2 |

## Measurement log

| Date | Milestone | Largest file | Total lines | DOM update sites | State count | Race guards | Over threshold | Conclusion |
|---|---|---|---|---|---|---|---|---|
| 2026-07-26 | M1 | style.css 158 / app.js 175 | 393 → 534 | ~15 (the first entry counted only innerHTML=8; the counting rule has since been corrected) | 4 | 1 (openPR generation counter) | none | Stay on A |
| 2026-07-26 | M2 | annotate.js 392 | 1007 | **~40 (grep, all three categories; over line #3=25)** | 6 (right at line #4) | 2 (openPR generation + submit busy) | **#3 tripped (1 indicator)** | Per the rule, one indicator is recorded, not migrated. Note on the call: those 40 update sites are concentrated inside explicit `render()` rebuild paths (each `append` inside constructors like `buildCard` counts individually) rather than scattered incremental patches — the shape of the complexity is gentler than the number. M3 (comment threads + optimistic updates) will very likely trip #5 or #9 as well; two at once → migrate |
| 2026-07-26 | M2, after review fixes | annotate.js ~440 | ~1080 | ~42 (#3 still over) | 6 | **4: openPR generation / openDoc per-tab generation / submitAll completion snapshot / busy (#8 over at >3)** | **🔔 #3 + #8 both tripped → gate is live** | **Per the §8.1 rule: pause new features, do the A→B (Preact + Vite) migration before M3 starts.** What the review backs this up with: both real bugs this round (⌘Enter bubbling into an immediate submit, completion callback not validating the world) were event-propagation and async-timing bugs — exactly the mental load a framework takes over. Portable assets: the anchoring math, hunk parsing, the GitHub API wrapper, all the CSS |
| 2026-07-26 | **Migration done (same night)** | ui.js 487 | 880 (−200) | ~7 (only the document island's innerHTML + layoutCards positioning — everything else belongs to the vdom now) | unchanged (now carried by hooks) | 3 hand-written guards (openPR stale checks ×2 + submit snapshot); the document-load race is now structurally solved by effect cleanup | Gate executed | Form: htm/preact standalone, vendored (one 13KB file). **Zero-build is preserved**: push is deploy, no npm dependency chain. Vite waits until JSX/TS is genuinely needed (the literal departure from SPEC §8's B has been recorded in the §8 revision). All review fixes retained; `?demo=1&auto=1` passes smoke through real event paths (fence line number 16, exact). Indicators stay on the same table with unchanged thresholds |
| 2026-07-26 | **M3 (comment threads + reply display / rev switching)** | ui.js **641** (< 800, #1 not tripped) | 1164 (includes render.js; excludes vendor; < 2500, #2 not tripped) | **16** (grep, all three categories, ui.js only; **fourth-round review re-check: M3 added a net 0** — under the same counting rule it was already 16 before this milestone started, the 13→16 rise happened during review fixes, and the migration row's ~7 was rule drift; < 25, **#3 not tripped**) | 17 `useState` in App (4 new: commits/comments/viewed/otherOpen) — but what actually needs **cross-page sync** is still the cur/docPath/drafts tier; all 4 new ones are same-page derived state, so #4 is not tripped in substance | still 3 tiers of hand-written guards: the document island's dead flag (deps now include viewed, so a rev switch reuses the same cleanup) + the `cur.pr.number` world snapshot in openPR/loadComments/submitAll + submit busy; thread refresh rides the inline world guard inside loadComments and adds no new guard category | **M3 added 0; fourth-round review re-check: #8 under the original per-site rule is 5–6 sites, still over; #4 under the total-state rule is 17 > 6, over** — the gate has already been executed and there is no tier above B, so no further action; both indicators get redefined before v1 | **Honest conclusion: the gate memo predicted M3 would trip #3/#5/#9 — not one of them fired**, because the migration structurally deleted the "hand-written DOM sync" cost layer (which was the entire point of moving to Preact). M3's comment threads and revs all render declaratively through the vdom, so DOM update sites didn't grow (net 0). On #9 optimistic rollback: M3's "re-fetch the thread after submitting" is a **read refresh**, not an optimistic write — no rollback path, so it doesn't count toward #9. All 5 smoke checks green (including switching to an old rev, where the body shrinks and floating comment entry is disabled). Thresholds unchanged; keep measuring against the table |
| 2026-07-27 | Search + archive + conversation-comment display + settings fixes (user feedback day) | ui.js **776** (previously mis-recorded as 802 — an estimate booked without measuring, now corrected; < 800, **#1 not tripped but right at the line**) | 1585 | not recounted (all increments went through declarative vdom + search.js as a pure logic layer) | +4 (q/hits/searching/tab and friends, all same-page derived state) | unchanged | none | Plan for being at the line: before the next feature starts, split the sidebar and reading area into subcomponent files (search orchestration is already extracted into search.js — same discipline). One lesson recorded: **measurement rows must come from real command output; estimates are not allowed** |
| 2026-07-28 | Sixth-round review back-accounting (F6–F9 + search/archive/conversation display — **the ledger stalled across 6 commits**) | **ui.js 904 (#1 tripped, and unbooked for 7 consecutive commits)** | JS-only **2015**; with style.css + index.html **2426** (just 74 lines from #2's 2500) | 14 (grep, three categories; < 25, not tripped) | 26 `useState` in App (threshold 6 — long over under the total-count rule) | mechanism still 3 tiers, ~7 by per-site counting | **#1 tripped; #2 closing in; #4 persistently over under the total-count rule** | **The most expensive finding this round wasn't a bug, it was the process failing.** Commit 3c0416a had just written "plan for being at the line: split into subcomponents before the next feature" and "measurement rows must be measured" — then 5 features landed back to back with neither a split nor a measurement (776→817→…→904). **Counting rules, fixed from here on**: #1/#3/#4 look at ui.js and at counts inside App; #2 uses "JS + css + html, excluding vendor" = 2426 today. **Next hard constraint: no new features are accepted until ui.js is split into subcomponents** (sidebar / reading area / comment rail). The freeze period wasn't going to add features anyway — it's exactly the window to do the split |
| 2026-07-28 | **Paying it back: ui.js split into subcomponents** | **ui.js 741 (< 800, #1 cleared)**; plus five new files under components/ (cards 64 / sidebar 50 / topbar 34 / setup 38 / other-threads 32) | 2481 all-in (incl. css + html, same rule as the row above) | not recounted (the split only relocates code, it adds no write sites) | unchanged | unchanged | **none (#1 cleared)** | How it was cut: along the lines of "can be lifted out whole and has a small dependency surface" — the three comment cards, the setup page, the sidebar, the top bar, the collapsed group of other threads. Side benefit: once props became explicit, each piece's dependency surface is visible at a glance (sidebar takes 15, top bar 13 — which is itself the signal for what to tighten next). **Process patch: the measurement is now printed by the pre-push hook**, so every push forces you to see ui.js's line count and the all-in total. No more relying on remembering to book it |

## Memo

- **2026-07-28 process retro**: the ledger went 7 commits with nobody updating it, and #1 was tripped that entire time. The lesson isn't "we forgot" — it's that **booking had no trigger**. Feature commits and measurement commits were separate, held together by nothing but good intentions. The hedge: after the subcomponent split, `wc -l src/ui.js` goes into the pre-push hook's output (non-blocking, print only), so every push is forced to see the number.

- Indicators 8 and 9 were added after the dual-track review: all seven original indicators measure **static complexity** (line counts, update sites, state counts), while what actually forces you onto a framework is **concurrent writes**. openPR's race already consumed site #1 as of that day.
- The hedge worth doing whether or not you migrate: a single state object + an explicit `render()` + generation-counter guards (about 50 lines). The genuinely expensive part of a migration is prying state out of event closures, and that bill comes due either way.
- The gate was expected to trip at **M3** (comment threads + post-submit refresh + optimistic marking) on indicators 3/5/9 — that wouldn't be a failure, it's precisely how §8.1 is designed to be used.

| 2026-07-28 | **Gate re-axed (#1/#2 demoted)** | — | — | — | — | — | **不再按行数判触发** | 复盘定案：拆子组件让复杂度下降、行数上升——#1/#2 与目标反向，且**行数类指标全程从未预测过任何一个 bug**（真兑现过的是迁移轮的 #3+#8）。故降级为描述性统计：pre-push 继续打印看趋势，不再当触发条件；7/28 那次「#2 触发」随之作废。新增 SPEC §8.2「后端触发条件 S1–S4」补上真正悬着的那条轴（零后端何时不再成立）。首次评估结论=不建后端，三条更便宜的路记为 BACKLOG F10/F11 与 F5 |
