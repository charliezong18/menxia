[English](BACKLOG.md) · **中文**

# BACKLOG

已想清楚但还没排期的东西。

**排期规矩（2026-07-27 更新）**：v0 三个里程碑（M1/M2/M3）已全部完工，原「先做完闭环再动这里」的前提已失效。现行规矩改为——**功能冻结到 8/8 结账**：那天看的是真实使用（呈了几折、批了几条），不是功能表。冻结期内只修 bug 与挡路的摩擦，不加新 F。

---

## F1 · 折首摘要（TL;DR）

**要什么**：每折开头自动生成一段摘要——不管这折从哪儿 feed 进来（spec / issue / PR / 日报），进朱批之前先有个"这折说什么、要你拍什么"。

**为什么**：长文档最大的摩擦不是读不动，是**不知道值不值得读**。摘要把"要不要现在读"这个决定的成本降到 10 秒。

**怎么做**：agent 侧生成（review-loop skill 在开 PR 时就写好），存进 PR body 或文档 frontmatter；朱批只负责渲染成折首的摘要卡。**app 内不调 LLM**（见下方架构边界）。

**待定**：摘要要不要分两段——「这折说什么」+「要你拍什么」（后者其实就是现在 PR body 的「待你拍板」，可以直接升级成结构化字段）。

**✅ 已拍板（2026-07-31，评估折 #60）**：通过，8/8 冻结解除后第一个做。评估折用数字坐实了断层——写入侧 schema 强制五段 body（tldr／待拍板／怎么用／去向／直链），阅读器**一段都不渲染**（`pr.body` 全程只被读一次，用来抽 session 标记）。范围钉死在便宜形态：**把 PR body 里现成的 TLDR＋待拍板渲染成摘要卡**（列表页与/或折首）——不加 frontmatter、不加新数据、不调 LLM。当年的「待定」顺带定案：分两段，且正是现有的结构化字段。

---

## F2 · 臣拟朱批（agent 按你的 background 先粗筛一遍）

**要什么**：文档呈上来之前，agent 先以「Charlie 的视角」通读一遍——他是程序员、在做 X 项目、关心 Y——把可能的问题预先批在原文对应的句子上。你打开时，右缘已经有一列灰色的「臣拟」批注等着。

**为什么**：这是这个产品**唯一一件 Google Docs、GitHub、Notion 都做不到的事**。它们能给你更好的批注 UI，但没有一个能在你读之前替你读一遍。你的时间瓶颈从来不在"写批注"，在"发现该批哪里"。

**怎么做**：agent 在呈折时顺带跑一遍 background-aware review，把结果作为**普通 inline comment** 发到 PR 上，正文带 `臣拟` 标记（比如首行 `> 臣拟`）。朱批渲染时识别这个标记，用灰底、比真朱批低一档的视觉呈现。**app 依旧不调 LLM**。

**背景从哪来**：一份 `background.md`（职业、在做的项目、技术偏好、review 时最在意什么），放 review repo 里，agent 每次读它。这份东西 memory 里已经有大半，可以直接导出。

**风险（要防的）**：预填批注会**锚定你的判断**——人看见现成意见容易顺着点头，反而少想。缓解：臣拟批注默认折叠成一行标题，你读完自己那遍再展开；或者提供"先蒙上"开关。这条不解决就别做 F2。

**✅ 已拍板（2026-07-31，读物折 #15 涂归）**：锚定风险的缓解取「先蒙上」——臣拟批注默认整列蒙住，你读完自己那遍再揭开。另立一条实施要求：**评审配置化**——呈折时可配臣拟评审的类型与模型：类型按折子性质分（代码类／设计类等，各配各的评审视角），模型备选（Fable、GPT 5.6、Gemini 等）。配置与调用全在 agent 侧，门下照旧只认标记渲染，架构边界不动。

---

## F3 · 一键认同 / 加一

**要什么**：臣拟批注旁边给按钮——「准」（认同，转成正式朱批呈回）、「加一」（在它基础上追一句你的话）、「驳」（不认同，丢弃）。不用自己敲字。

**为什么**：F2 的价值只有在**采纳成本≈0** 时才兑现。要你重新打一遍字，等于没帮上忙。

**怎么做**：纯前端 + GitHub API——「准」= 把臣拟 comment 的内容原样重发一条正式朱批（或直接给原 comment 加 👍 reaction + 标记为已采纳）；「加一」= 打开批注框并预填引用；「驳」= 本地隐藏 + 记一次负反馈（可用于改进 F2 的 prompt）。

**待定**：「准」是转成你名下的新批注，还是给臣拟批注盖个"御准"戳？前者对 agent 更清晰（它只认正式朱批），后者更省事。倾向前者。

**✅ 已拍板（2026-07-31，读物折 #15 涂归）**：取前者——「准」＝转成 Charlie 名下的正式涂归（agent 只认正式涂归，语义不变）。本项并入 F2 的实施范围，不单独排期。

---

## 架构边界（F1–F3 共同前提，先定死）

SPEC §3 的非目标写着「AI 集成——agent 侧由 review-loop skill 全权负责，朱批不知道 AI 存在」。F1–F3 看似违反它，实则不必：

**所有智能都在 agent 侧离线算好，以普通 GitHub 数据（PR body / inline comment）的形式送进来；朱批只负责识别标记并渲染成合适的样子。**

这样守住三件事：①朱批仍是零后端纯静态站，不需要任何 API key 在浏览器里；②agent 换模型、换 prompt 不影响前端；③朱批挂了，臣拟批注照样在 GitHub 上看得见。

代价：不能做"读到一半问一句"这种实时交互——那要 app 内调 LLM，是另一个量级的产品（要后端或要用户自带 API key）。真需要时，走 SPEC §8.1 里 C 方案的触发条件评估。

---

## F4 · 版本（rev）浏览与版本间 diff（2026-07-26 口头提及，先对齐记录）

**记录澄清**：拍板②定的是 v0 **不做**「v1→v2 对比视图」；北极星的 diff 奏折看的是代码 PR 的新旧两侧，不是文档版本对比。所以「看 rev / diff between revs」目前**不在**任何已排期的里程碑里——但地基已经在：每轮改稿都是 PR 分支上的一个 commit，`getFileText(path, ref)` 本来就支持任意 commit SHA，缺的只是 UI 入口。

三档实现，从便宜到贵：

1. **rev 切换**（~小时级）：用 PR commits API 列出历史版本，阅读页加版本下拉，切到任意旧版渲染。只读不比对。——**✅ 2026-07-26 拍板划进 M3**，不再属于 backlog。
2. **版本间源码 diff**（~半天）：任选两个 rev，复用北极星的 split-view 组件展示 markdown **源码** diff。组件白嫖，代价是看的是源码态（阅读体验降一档，胜在便宜）。
3. **渲染态 track-changes**（贵）：改动段落在渲染态上高亮 = 原拍板②的 B 方案，要做段落级 diff 算法 + UI。

**第 2/3 档触发条件**：rev 切换上线后，真实使用中「翻旧版也找不到改动点」发生 ≥3 次再上第 2 档；第 3 档除非前两档都被证明不解渴。

---

## F5 · 派人复核与签字（assign reviewer + sign-off）

**要什么**：把一折奏折指派给别人（同事 / 惠雪 / larry / 未来的团队）读并**签字放行**，签完才允许钦此。从「单人 review 台」长成「可问责的评审流程」。

**为什么现在记而不做**：v0 的全部设计前提是单人（朱批/总批/钦此都是"你一个人的动作"），派人复核会同时动到身份模型、权限模型和 UI 语义——不是加个按钮的事。但这是产品从"我的工具"走向"能给别人用"的第一道门，值得留位置。

**有意思的反转**：整个「钦此」按钮之所以存在，是因为 **GitHub 不允许作者批准自己的 PR**（试点第一天就撞见，PAIN.md 第一条）。而**别人是可以 Approve 的**——所以 F5 一旦落地，GitHub 原生的 review approval 机制立刻可用：签字 = 一条 `event: 'APPROVE'` 的 review，状态天然可查（`GET /pulls/{n}/reviews` 的 state=APPROVED）。**不需要发明自己的签字存储**，仍守着「智能/状态全在 GitHub、朱批只是镜片」的架构边界。

**怎么做（草案）**：
- 指派：`POST /pulls/{n}/requested_reviewers`（需要对方有仓库读权限——私有仓要么加 collaborator，要么这类协作折走另一个仓）。UI：折子头部一个「请…复核」。
- 签字：被指派人在自己的朱批里读完 → 一个「准奏」按钮 = `event: 'APPROVE'`；驳回 = `REQUEST_CHANGES`。这两个 event 值 submitReview 已经支持，只差 UI。
- 状态：清单页每折显示签字态（待你批 / 待某某签 / 已准奏 N/M）；**钦此按钮在「要求签字」的折上未签不给按**（软闸，本地判断，不是 branch protection）。
- 硬闸可选：真要不可绕过就上 GitHub branch protection 的 required approvals——那是仓库设置不是 app 功能，一行不用写。

**待拍板（真做时才问）**：①身份——别人用自己的 GitHub token 打开同一个朱批站点即可（app 无用户系统，天然多人），还是要认"当前用户是谁"来决定按钮？（前者几乎零成本：`GET /user` 拿 login 就够）②私有 review 仓怎么给外人读权限，还是协作折另开一个仓？③签字是不是必须——默认可选、只有标记了「需签字」的折才卡钦此。

**排在哪**：v1（移动端 + PWA）之后、北极星（diff 奏折）附近；单人流程被真实使用磨顺之前不动。

---

## F6 · 折间链接（2026-07-27 ✅ 已做）

**做了什么**：文档/批注里的**本仓 GitHub permalink** 被朱批拦下来，变成 app 内跳转（开折 + 滚到那一段 + 闪一下）；外链照常新窗口打开。顶栏「引用此处」把当前选中处拷成 `[「引文」](permalink)`，粘进另一条朱批即成链接。

**为什么用 GitHub 原生 permalink 而不是自创 `[[双链]]`**：私有语法会让 GitHub 上显示成乱码、agent 还得学它，"数据在 GitHub、朱批只是镜片"这条根就断了。用 permalink：GitHub 上是正常链接、agent 读得懂、朱批只是把它**拦下来**——链接图谱不是新数据，只是 markdown 里的普通 URL。

**明确没做的：内嵌预览**（悬停/展开直接看到目标内容）。风险是递归（A 嵌 B、B 嵌 A）与快照失效（目标改版后嵌的是旧的还是活的）。等链接真被用起来再说。

**已知取舍**：链接会随目标改版漂移——所以「引用此处」拷的是**带引文的链接**，漂了至少还知道当初指的是哪句（同锚定的引文兜底哲学）。

---

## F7 · 外部直达链接（deep link）（2026-07-27 ✅ 已做）

**要什么**：从朱批**外面**能一步跳到某一折——`.../zhupi/?pr=13`，需要时再精确到文档和行。

**为什么**：现在 agent 呈完折只能说「去清单里找第 13 折」，给不出可点的链接。2026-07-27 实测踩到：agent 发完朱批递了 GitHub 链接，Charlie 点开发现"不是 zhupi"——**因为除首页清单外，朱批没有任何可分享的 URL**。折子多起来之后每次都要人肉找；手机上从通知/微信/邮件跳进来的场景也全断。

**为什么便宜**：F6 已经把难的部分做完了——`link.js` 的 `parseZhupiLink()` 本来就能把本仓 permalink 解析成 `{prNumber, path, line}`，`buildRef()` 也已能反向生成。缺的只是**启动时读一次 URL 参数**再走同一条跳转路径。形态两选一（倾向都收）：

- `?pr=13`、`?pr=13&path=docs/a.md&line=42` —— 短、好念、agent 好拼
- `?ref=<GitHub permalink>` —— 把 F6 已认得的三种链接原样塞进来，零新语法

**顺带**：地址栏应随当前折/当前位置更新（`history.replaceState`），这样浏览器书签和 ⌘L 复制当前地址天然可用；顶栏「引用此处」可以多给一个「拷朱批直达链」的形态。

**待定**：分享出去的链接指「这折」还是「这折的这一句」——后者要把锚定信息编进 URL，与 F6 的引文兜底哲学一致，但 URL 会变长。倾向：默认到折，有选中文字时才带行号。

**做了什么（收全两种形态）**：`?pr=13`、`?pr=13&path=docs/a.md&line=42`、以及 `?ref=<GitHub permalink>`（复用 F6 的解析，零新语法）。进场即跳，归档折自动切「已钦此」栏；指向不存在的折会明说而不是白屏。地址栏随当前折/文档更新（`replaceState`，不污染返回键），所以 ⌘L 和书签天然可用。「引用此处」默认拷**朱批直达链**（点了直接进 app 读到那一段），按住 Alt 拷 GitHub permalink。

**排期后记**：条目原写「M2/M3 闭环优先，真要插队才插它」——落笔时那句已过期（v0 三个里程碑当天全部完工，前面没队了）。实际按建议直接做掉，用时约一小时，与预估一致。

---

## F8 · 语言切换（双语折的 中/EN 切页）（2026-07-27 ✅ 已做）

**要什么**：一折里同时存在中英两版时，折子顶部给一个 `中 / EN` 切换，点一下换语言、不离开当前折。默认中文，记住上次选择。

**为什么**：2026-07-27 真实使用中撞到（PR #12 总批原话：「你这个中英双语的我看着头疼，能不能变成 github read me 那种我可以切语言的」）。Charlie 的项目文档一律双语（对外要英文、他自己读中文），但**双语服务的是"有两种读者"，不是"一个读者读两遍"**——逐段 EN/中文交错时，读中文的人被迫全程用眼睛过滤掉一半内容，这是纯噪音。归 bug/摩擦类，不算冻结期新增功能。

**怎么做**：沿用 GitHub README 的多语言惯例——同一折里 `foo.md`（英文）与 `foo.zh-CN.md`（中文）互为语言变体。朱批已经拿得到一折的全部文件列表，检测就是「同 basename + `.zh-CN` 后缀」这一条规则；命中就在文档头渲染切换 chip，没命中就当普通多文档折照旧。本仓自己的 `README.md` / `README.zh-CN.md` 就是现成样例。

**为什么不在 app 里做"合并双语文档的语言过滤"**：解析交错双语正文得靠猜（哪段是英文哪段是中文、标题怎么配对），脆且必然误判。把拆分留在**内容侧**（agent 写文档时就拆两份互链文件，已升级为默认写法），朱批只做**视图切换**——和「智能全在 agent 侧、朱批只是镜片」的架构边界一致。

**批注归属**：朱批锚在 path+line 上，所以在中文版划的批注就落在中文文件上——这是对的，不需要跨语言同步批注。但要想清楚：**同一处意见在两版上会各批一次吗？** 目前判断不会（他只读中文版），若真出现再说。

**已做**：检测就一条规则——同 basename + `.zh-CN` 后缀（`foo.md` ↔ `foo.zh-CN.md`）。命中就在文档头出「中 / EN」chip，切换只换语言变体、不离开当前折；偏好记 localStorage，默认中文，开折时按偏好挑首篇。**文档 tab 里成对的只列一个**（否则中英两份并列 = 把噪音从正文搬到 tab 栏），标签去掉语言后缀，语言由 chip 表达。单语折一切照旧，不添任何控件。

**原待定的处置**：①切换后停在同一篇的顶部（没做按标题序号对齐位置——两版标题必须同构才成立，不值这个复杂度）②清单页不标「有双语」（chip 已在折内表达，清单再标是重复）③英文版不隐藏，只是默认不选中。

**旧待定原文**：①切换后是停在同一段落还是回到顶部（理想是按标题序号对齐位置，但两版标题必须同构才成立，可能不值这个复杂度）②清单页要不要标一折"有双语"③英文版是否该默认对 Charlie 隐藏，只在他主动切时才出现。

## F9 · 回奏对（批完跳回呈折的 Happy 会话）（2026-07-27 ✅ 已做）

**要什么**：折子顶栏一个常驻「回奏对」按钮，点开就是呈这折的那次 Happy 奏对。

**为什么**：批完得回去跟臣说「读批注」，而臣有几十个 session。人肉找回是哪一个既慢又常找错——批注写完了却卡在"发给谁"上。

**怎么做**：两半，都不用改 Happy。臣开折时把自己的 Happy 会话 id 埋进 PR body 一行 HTML 注释 `<!-- happy-session: <id> -->`（id 由 `~/.claude/skills/review-loop/happy-session-id.sh` 取：沿 ppid 往上爬，比对 `~/.happy/sessions.json` 各会话记的 `hostPid`）；朱批 `parseHappySession()` 从 body 认出来，渲染成指向 `…/happy/session/<id>` 的按钮。Happy 的 web 部署有 `404.html` SPA fallback，深链能直接进会话。

**取舍**：①**不自动跳**——提交朱批后不劫持页面，只留按钮（2026-07-27 拍板：最小改动，且他常连批几折）②注释而非可见链接：GitHub 上不占版面，臣只需 append 一行③标记里也可写整条 URL，fork 的人自架 Happy 不用改代码。

**已知缺口**：会话可能早被 reaper 回收或退出，链接就只剩存档可看、不能续话——朱批没有 Happy 的 API，判断不了活没活，只在 tooltip 里说明。要根治得让 Happy 支持 resume 深链。

---

## F10 · 折子到了推一条（GitHub Actions，不用后端）

**要什么**：agent 呈折 → 手机上收到一条通知，而不是靠你想起来去刷清单。

**为什么不用后端**：review 仓加一个 workflow（`on: pull_request: [opened]`）→ `curl` 打到 ntfy（Charlie 现成在用 `charlie-claude-notify-*`）。前端零改动，因为通知根本不经过 app。成本 0，维护面 = 一个 15 行 yml。

**代价**：Actions 有分钟级延迟——但「折子呈上了」本来就不是秒级需求。真被延迟误事 ≥3 次，那是 SPEC §8.2 的 S3 触发条件。

**注意**：这与被否掉的 attention inbox 不冲突。那条被否的是「把所有 AI 产出堆成信息流」；这条只推**一件事**：有折子等你批。P0 打断权只给这一类。

---

## F11 · 跨设备草稿：改用 GitHub 原生 pending review

**要什么**：在电脑上攒了一半的朱批，手机上打开能接着批。

**发现**（2026-07-28 后端评估的最大意外收获）：**GitHub 原生就有这个东西，我们没用**。`POST /pulls/{n}/reviews` 不带 `event` 会落成一条 **pending review**，可以后续追加 comment、最后再 submit。那就是服务端草稿——天然跨设备、天然属于同一个 PR、agent 侧也看得见。

**顺带解掉**：localStorage 那颗配额炸弹（第六轮评审的阻断项）。草稿不再是本地唯一不可再生的数据。

**代价**：要处理「本地草稿 ↔ 服务端 pending review」的合并语义（同一条批注两处都有怎么办、离线时怎么办）。中等复杂度，但**完全不需要新增基础设施**。

**待定**：本地草稿是彻底改成 pending review，还是本地当离线缓存、上线即同步？倾向后者——离线仍可批是现在的一条真优点，别弄丢。

---

## 后端评估结论（2026-07-28）

**不建。** 完整评估见 review 仓 `docs/zhupi-thin-backend-eval.md`（PR #16）。四个候选能力里三个可在纯静态档解决：推送 → F10；跨设备草稿 → F11；多人签字 → F5 已分析过（朱批没有用户系统，天然多人，签字用 GitHub 原生 APPROVE，门槛是仓库权限不是后端）。唯一真需要托管密钥的「app 内实时问一句」需求本身未经验证。

触发条件 S1–S4 已写进 SPEC §8.2，此后「要不要加后端」直接引用该节结案。

---

## F12 · 携卷——一键拷走上下文，喂任何 agent（2026-07-31，出自评估折 #60）

**要什么**：回奏旁加一个「携卷」键：一键把当前折拼成自包含 markdown——折号＋标题＋正文全文＋全部涂归线程（引句、批语、回话）＋待拍板——放进剪贴板，粘给**任何** agent 都能接着干。

**为什么**：回奏赌的是「那个会话还活着」；2026-07-29 已定调真兜底是自包含文档，携卷把这句定调做成了产品。剪贴板是所有 agent 都认的唯一集成面，顺带把 MCP 侧被降级的「跨 harness 零消费者」收益零成本捞回来。Charlie 在 #60 上提出。

**怎么做**：纯前端拼装——pr／docs／threads 本来就全在内存里；不加新数据、不加 API 调用。

**状态**：#60 已拍板；照 F10/F11 先例冻结期内只记录，8/8 结账后动工。

---

## F13 · 「需核实」标记渲染（2026-07-31，出自评估折 #60）

**要什么**：agent 在正文里对未核实结论统一标固定记号（如【需核实】）；阅读器识别并渲染成醒目状态（虚线下划线＋角标），列表页给「本折 n 处需核实」计数；写入侧 lint 补一条 warn，呈折时就知道数量。

**为什么**：未核实结论是 AI 文本最独特的性质，而阅读器现在对它无感（#60：「不可信性与体量——一个都没碰」）。约定其实已存在一半——外包调研的家规本就要求打这个标；只是阅读器看不见。

**怎么做**：与 F2 的「臣拟」记号同一套路——智能留在 agent 侧，zhupi 只认记号、只管渲染。符合上方架构边界。

**状态**：#60 已拍板，排 F1 之后；8/8 结账后动工。

**做了什么**（只做阅读器侧；写入侧 lint 归 menxia-mcp）：

- 新增纯函数模块 `src/verify.js`。`countVerifyMarkers(text)` 是「什么算一处」的唯一事实源；`decorateVerifyMarkers(root)` 管渲染。
- **token 识别**（固定 token，不做模糊匹配——认两种写法）：
  - `【需核实】` —— 方括号标记本体
  - `需核实：` / `需核实:` —— 标记后紧跟中/英文冒号（冒号说明式）也算一处
  - 除此之外一概不认。光是「需核实」二字（无冒号、无方括号，如疑问句「需核实吗」）不算，免得误报冲淡信号。
  - **围栏代码块（``` / ~~~，容 0–3 空格缩进）与行内代码（`` `...` ``）内不算** —— 那里的 token 是示例不是声明。写进单测钉死。
- **渲染只在纯文本层做——`render.js` 的 `html:false` 分毫未动。** `decorateVerifyMarkers` 用 `TreeWalker` 遍历文本节点（跳过 `<code>`/`<pre>`），把每处匹配包成 `<span class="verify-marker" title=…>` ＋一个 `<sup class="verify-tag">?</sup>` 角标，全程只用 `createTextNode`/`createElement`——零原始 HTML 注入。在 `ui.js` 里 `el.innerHTML = renderMarkdown(text)` 之后紧接着调用。CSS：朱砂虚线下划线 + 小上标角标。
- **阅读页**：顶栏面包屑显示当前打开这篇文档的「需核实 N」计数（0 则不显示）。
- **列表页**：每张折卡显示「⚑ N」计数角标（0 或还没数过则不显示）。计数复用**已有取文路径**（`search.js` 的 `buildIndex`——任何一次搜索之后全文索引已在内存里），**不新增逐折请求**。中英成对文档（`foo.md` ↔ `foo.zh-CN.md`）按 base 归并、取最大值只计一次，同一批声明不重复算。**取舍**：该索引只有用户搜过一次才建，所以列表角标在首次搜索后才出现；从没搜过的折不显示角标——这是任务约定的退路（列表角标为机会式；阅读页在打开时始终显示精确的单篇计数）。
- **测试**：`test/verify.test.js`（11 条单测：方括号命中、冒号变体、模糊反例、无标记、多标记、围栏块排除、`~~~`/缩进、行内代码排除、落单反引号）。demo 文档带一处标记当浏览器冒烟靶；新增 2 条冒烟断言（`verify-marker-rendered`、`verify-count-in-topbar`）。全套绿：126 单元 + 22 DOM + 65 冒烟。

---

## F14 · 长折 TOC／分级阅读（2026-07-31，出自评估折 #60）

**要什么**：阅读页从标题自动生成悬浮大纲；多章折（#31：22 章）给章节列表视图。

**为什么**：体量是 #60 断层的另一半——22 章的书没大纲根本没法导航。

**怎么做**：渲染后的标题本来就带 `data-line`；纯前端。

**状态**：#60 已拍板，排 F1 之后；8/8 结账后动工。
