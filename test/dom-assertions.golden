fence 内第一行代码
fence 内第二行代码 → 精确到该行（fence 首行 +1 +换行数）
fence 行号落在 <code> 上且指向 ``` 行
h1 行号
h2 行号
id 与 data-line 并存（涂归的锚不能被页内锚顶掉）
table 行号
不开 headingIds 时一个 id 都不打
划中内联格式内的字仍锚到源行（文本搜索会失败，DOM 主锚不会）
列表项行号
双链只给锚＝本篇内跳转（不拼成 .md#…那种死链）
双链带文件名时 href 仍是「文件 + 锚」（点击暂只切篇不落锚）
取该行之前最近的标题
同名标题去重（第二个追加 -1）
图外面套着链接时不劫持（让位给链接本身）
图片独占的标题不打 id（不把 URL 灌进 slug）
块级元素带 data-line-end
岛屿 innerHTML 换掉后仍认（委托扛得住 replaceChildren）
引文不存在时返回 null（不乱画高亮）
引文取渲染文本（不含 markdown 记号）
撞词：按块内偏移定位到第二处而非永远第一处
无选区 → 不产生锚
标题按 GitHub 口径打 id（CJK 标点全去掉）
标题里的链接只取可见文字，不把 URL 算进 slug
段落内划选 → 段落行号
段落行号
点正文图 → 开展图窗，带上 src 与 alt
第一节之内取 h1
第二个表格行独立成行
绝对 URL 的图同样能点开（委托覆盖全部 img，不只相对路径那批）
缩进代码块第二行 → 精确到该行（+0 偏移）
缩进代码块行号落在 <pre> 上且是首行
表格 tr 有独立行号（缺它则批表格只能锚到整表首行）
表格第二行划选 → 该行行号
解绑后不再触发（effect 清理是真的清掉了）
